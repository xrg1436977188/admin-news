import {setToken, getToken,removeToken} from './token'
import { setMenuList,getMenuList,removeMenuList } from './menu'
import request from './request'
export{
    request,
    getToken,
    setToken,
    removeToken,
    setMenuList,
    getMenuList,
    removeMenuList
}